@page "/finance"
@inject HttpClient Http

<h1>Finance</h1>
<p>@status</p>

@code {
    private string status;

    protected override async Task OnInitializedAsync()
    {
        status = await Http.GetStringAsync("https://jvc-app-webapi.herokuapp.com/api/finance/status");
    }
}