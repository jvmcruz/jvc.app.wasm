@page "/finance"
@inject IFinanceService Service

<h1>Finance</h1>
<p>@status</p>

@code {
    private string status;

    protected override async Task OnInitializedAsync()
    {
        status = await Service.GetStatusAsync();
    }
}