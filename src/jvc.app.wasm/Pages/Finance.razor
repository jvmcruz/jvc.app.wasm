@page "/finance"
@inject HttpClient Http

<h1>Finance</h1>
<p>@status</p>

@code {
    private string status;

    protected override async Task OnInitializedAsync()
    {
        status = await Http.GetStringAsync("/api/finance/status");
    }
}